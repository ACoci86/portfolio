// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // CHANGE ME once the domain is decided. This is the one place the live URL is
  // set. Link previews (Open Graph) need absolute URLs, so they will point at
  // the wrong host until this matches where the site is actually served.
  site: "https://portfolio.pages.dev",
});
